// === Scorekeeper Scorecard Configuration ===
// After deploying the Apps Script web app (see README.md), paste its URL here.
// SUBMIT_KEY and ADMIN_KEY must match the values you set inside Code.gs.
// Leave APPS_SCRIPT_URL empty ("") to run the site in offline-only mode (export JSON, print).

window.SCORECARD_CONFIG = {
  APPS_SCRIPT_URL: "",
  SUBMIT_KEY: "",   // light spam guard for submissions
  ADMIN_KEY: ""     // password required to view the Submissions list
};
